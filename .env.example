OPENROUTER_API_KEY=
HERMES_INFERENCE_PROVIDER=openrouter

# Web UI basic auth (set at least one)
DASHBOARD_PASSWORD=
# DASHBOARD_PASSWORD_HASH=  # bcrypt, alternative to plaintext above
DASHBOARD_USER=admin

# Optional integrations
TELEGRAM_BOT_TOKEN=
